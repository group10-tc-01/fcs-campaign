apiVersion: v1
kind: Service
metadata:
  name: fcs-campaign-service
  namespace: fcs-campaigns
  labels:
    app.kubernetes.io/name: fcs-campaign
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: fcs-campaign
  ports:
    - name: http
      port: 8080
      targetPort: http
